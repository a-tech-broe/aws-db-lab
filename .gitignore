# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
crash.*.log
.terraform.lock.hcl.bak
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
tfplan
*.tfplan

# Variable files may hold account IDs, emails and CIDRs
*.tfvars
*.tfvars.json
!*.tfvars.example
# *.auto.tfvars is committed on purpose: CI needs the non-secret settings, and
# local + CI must agree or every pipeline plan shows phantom drift.
!*.auto.tfvars

# Local tooling
.env
.envrc
.DS_Store
.vscode/

# Lab output
out/
*.log
